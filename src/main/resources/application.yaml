server:
  port: 8082
  servlet:
    context-path: /users
spring:
  application:
    name: authentification-service
  datasource:
    url: jdbc:postgresql://localhost:5432/db_users
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  main:
    allow-circular-references: true
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    port: 587
    username: elhadjbs59@gmail.com
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
